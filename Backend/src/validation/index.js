const Validator = module.exports;

Validator.statusValidator = require('./status.validator');
Validator.AuthValidator = require('./auth.validator');
Validator.ChatValidator = require('./chat.validator');
